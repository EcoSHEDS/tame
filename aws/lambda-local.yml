AWSTemplateFormatVersion: 2010-09-09
Description: Lambda resource stack for TAME
Parameters:
  functionName:
    Type: String
    Default: tame-dev-lambda
  databaseArn:
    Type: String
    Default: arn:aws:dynamodb:us-east-1:474916309046:table/tame-dev-projects
  storageArn:
    Type: String
    Default: arn:aws:s3:::tame-dev-storage
  roleName:
    Type: String
    Default: tame-dev-lambda-role
  policyName:
    Type: String
    Default: tame-dev-lambda-policy
Resources:
  LambdaFunction:
    Type: "AWS::Lambda::Function"
    DependsOn: LambdaExecutionRole
    Properties:
      FunctionName: !Ref functionName
      Tags:
        - Key: project
          Value: tame
        - Key: env
          Value: dev
      Handler: lambda.handler
      Environment:
        Variables:
          REGION: !Ref "AWS::Region"
          TABLE_NAME: !Select [ 1, !Split ["/", !Ref databaseArn]]
      Role: !GetAtt LambdaExecutionRole.Arn
      Runtime: nodejs12.x
      Timeout: "25"
      Code: C:/Users/jdwalker/Projects/tame/api
  LambdaExecutionRole:
    Type: "AWS::IAM::Role"
    Properties:
      RoleName: !Ref roleName
      Tags:
        - Key: project
          Value: tame
        - Key: env
          Value: dev
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service:
                - lambda.amazonaws.com
            Action:
              - "sts:AssumeRole"
      PermissionsBoundary: !Sub arn:aws:iam::${AWS::AccountId}:policy/csr-Developer-Permissions-Boundary
  LambdaExecutionPolicy:
    Type: "AWS::IAM::Policy"
    DependsOn: LambdaExecutionRole
    Properties:
      PolicyName: !Ref policyName
      Roles: [!Ref LambdaExecutionRole]
      PolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Action:
              - "logs:CreateLogGroup"
              - "logs:CreateLogStream"
              - "logs:PutLogEvents"
            Resource: !Sub
              - >-
                arn:aws:logs:${region}:${account}:log-group:/aws/lambda/${lambda}:log-stream:*
              - region: !Ref "AWS::Region"
                account: !Ref "AWS::AccountId"
                lambda: !Ref LambdaFunction
          - Effect: Allow
            Action:
              - "dynamodb:GetItem"
              - "dynamodb:Query"
              - "dynamodb:Scan"
              - "dynamodb:PutItem"
              - "dynamodb:UpdateItem"
              - "dynamodb:DeleteItem"
            Resource: !Ref databaseArn
          - Effect: Allow
            Action:
              - "s3:putObject"
              - "s3:deleteObject"
            Resource: !Join ["", [!Ref storageArn, "/*"]]
Outputs:
  Region:
    Value: !Ref "AWS::Region"
  Name:
    Value: !Ref LambdaFunction
  Arn:
    Value: !GetAtt LambdaFunction.Arn
  LambdaExecutionRole:
    Value: !Ref LambdaExecutionRole
